.header-modul
    width 100%
    height 8%
    height 8%
    background white
  .header
      display flex
      align-items center
      width 100%
      height 8%
      cursor pointer
      position fixed
      left 0
      top 0
      background white
      z-index 1
      // background white
      .login-register
          display: flex
          flex-direction: row
          width 150px
          height 100%
          align-items center
          justify-content: space-around
      .header-p
          color rgb(87,197,247)
.transtation-moule
    display: flex
    width 100px
    flex-direction: column
    justify-content: center
    align-items: center
    .solid
        width:50px
        height:2px   
        background black 
.slide-fade-enter-active {
  transition: all 1s ease;
    opacity :1
}
.slide-fade-leave-active {
  transition: all 1s cubic-bezier(1.0, 0.5, 0.8, 1.0);
}
.slide-fade-enter, .slide-fade-leave-to
/* .slide-fade-leave-active for below version 2.1.8 */ {
  transform: translateX(-80px);
  opacity:0;
}

.slide-fadeed-enter-active {
  transition: all 1s ease;
    opacity :1
}
.slide-fadeed-leave-active {
  transition: all 1s cubic-bezier(1.0, 0.5, 0.8, 1.0);
}
.slide-fadeed-enter, .slide-fade-leave-to
/* .slide-fade-leave-active for below version 2.1.8 */ {
  transform: translateX(80px);
  opacity:0;
}
.top-fadeed-enter-active {
  transition: all 1s ease;
    opacity :1
}
.top-fadeed-leave-active {
  transition: all 1s cubic-bezier(1.0, 0.5, 0.8, 1.0);
}
.top-fadeed-enter, .slide-fade-leave-to
/* .slide-fade-leave-active for below version 2.1.8 */ {
  transform: translateY(-80px);
  opacity:0;
}
.showUser
  width 100%
  display flex
  justify-content space-around
.seticon
    display flex
    justify-content space-around
    align-items center
.login-content
    display flex
    flex-direction column
    justify-content space-around
    align-items center
    height 150px
.scale-down
    margin-top 10px
.upload-box
    height 58px
    width 100%
    display flex
    justify-content center
.upload-img
    width 58px
    height 58px
    overflow hidden
    border-radius 50%
    img
        width 100%
        height 100%
.showLogin
  width 100%
  height 100%
  display flex
  align-items center
